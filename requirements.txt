# MagnoGlove – Python Dependencies
# Install with:  pip install -r requirements.txt

# ── Computer Vision ────────────────────────────────────────────
opencv-python>=4.8.0          # Webcam capture, frame processing, display
mediapipe>=0.10.0             # Real-time hand landmark detection (21 points)

# ── 3D Simulation ──────────────────────────────────────────────
ursina>=5.0.0                 # 3D game engine – scene, entities, update loop

# ── Math / Numerics ────────────────────────────────────────────
numpy>=1.24.0                 # Vector math for gesture geometry

# ── Standard library (no install needed) ───────────────────────
# threading   – background gesture detection thread
# math        – sin/cos for animation
# time        – startup delays in main.py
